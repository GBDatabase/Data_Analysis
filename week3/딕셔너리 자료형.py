# 사전의 형태로 자료 저장
# 키,값
# 중괄효를 사용하여 선언하고, 키아 값이 쌍으로 구성
#
# 활용사레
# 전화번호, 영어사전, 주요역사연동프로그램


# 시퀀느 자료형이 아니어서 반복문에서는 사용할 수 없음
# 자료 추가시 있는 자료면 업데이트


#키에 해당하는 값이 있으면
# 딕셔너리명과 딕셔너리명(키)는 결과가 같다
#
# 딕셔너리명[키]: 없는 키를 호출하면 오류 밠ㅇ
# 딕셔너리.get(키): 없는 키를 호출하면 반환하지 않음
#     키가 있는지 없는지 모르면 get함수를 이용
#
# 키가 있는지 없는지는 in을 사용해서 확인
# 딕셔너리에 키가있다면 트루 없으면 펄스

#